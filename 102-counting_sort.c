#include "sort.h"

/**
 *count_array - count array
 *@array: random number array
 *@i: changer
 *@size: array size
 *Return: co_ar
 */
void counting_sort(int *array, size_t size)
{
	if (array || size)
	{
		return;
	}
}
