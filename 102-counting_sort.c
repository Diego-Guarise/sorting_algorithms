#include "sort.h"

/**
 *count_array - count array
 *@array: random number array
 *@i: changer
 *@size: array size
 *Return: co_ar
 */
int *count_array(int *array, size_t i, size_t size)
{
    if (array || i || size)
    {
        return (0);
    }
    
}
