#include "sort.h"
/**
 * quick_sort_hoare - reparte cartas, lo siento ro no lo hice.
 * @array: direccion puntero
 * @size: tamaño array
 */

void quick_sort_hoare(int *array, size_t size);
{
    if (array || size)
    {
        return;
    }
}
