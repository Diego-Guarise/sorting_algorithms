#include "sort.h"

/**
 *intercambiador_shell - intercambia ints
 *@array: random number array
 *@l: integer
 *@size: array size
 *@intervalo: interval
 *Return: void
 */
void shell_sort(int *array, size_t size);
{
	if (array || size)
	{
		return;
	}
}
