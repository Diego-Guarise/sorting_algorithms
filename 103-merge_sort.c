#include "sort.h"

/**
 *print_merge - print and merge
 *@array: random number array
 *@p1: changer 1
 *@p2: changer 2
 *@size: array size
 *Return: void
 */
void merge_sort(int *array, size_t size);
{
	if (array || size)
	{
		return;
	}
}
